# AGENTS.md

本文件为在本仓库工作的 AI 编码代理（Coding Agent）定义行为准则。无论是人类主动发起的任务，还是代理自主规划的步骤，凡涉及本文件所列范围，均须严格遵守。

核心原则一句话：**代理只执行确定安全的操作，其余一律先询问。**

## 快速索引

| # | 规则 | 说明 |
|---|------|------|
| 1 | [命中停下询问原则](#1-命中停下询问原则) | 与任何禁止项存在沾边可能，即停下来询问 |
| 2 | [禁止 Git 写操作原则](#2-禁止-git-写操作原则) | 只读命令可直接执行；写操作须逐次获得授权 |
| 3 | [禁止擅自推断人类意图原则](#3-禁止擅自推断人类意图原则) | 严格按指令执行，不做推测 |
| 4 | [禁止自动运行代码原则](#4-禁止自动运行代码原则) | 运行任何程序前，先向人类确认 |

---

## 规则

### 1. 命中停下询问原则

**只要代理的行为与本文件中任何禁止项存在沾边可能，即视为命中规则，应当先停下来询问人类。**

代理无需自行判断"这算不算写操作""这会不会影响仓库状态""这算不算运行代码"——**只要有一丝可能沾边，就先停下来问人类。**

- 不确定某个 Git 命令是不是写操作？**停下来问。**
- 不确定某个路径是否应该触碰？**停下来问。**
- 不确定某个命令算不算"运行代码"？**停下来问。**
- 不确定人类是否已在运行进程？**停下来问。**
- 不确定这件事该不该做？**停下来问。**

代理唯一需要预先判断的问题是：**这件事是否应该先问人类。** 答案只要倾向于"是"，就问。

多次确认的成本，远低于一次误操作。

---

### 2. 禁止 Git 写操作原则

**除非人类明确、单独地允许，否则代理不得执行任何形式的 Git 写操作。**

包括但不限于：

- `git commit`、`git push`、`git merge`、`git rebase`、`git cherry-pick`
- `git checkout`、`git reset`、`git tag`、`git stash`
- `git branch`（创建 / 删除 / 重命名）
- 以及任何会改变仓库状态、工作区、暂存区或提交历史的命令

代理只允许执行**只读**命令，例如 `git log`、`git status`、`git diff`、`git show`、`git branch -v` 等。

授权规则：

- 任务需要写操作时，必须**先询问人类并获得明确许可**，再执行。
- 许可**逐次生效，不可继承**：即使人类说过"帮我合并""帮我推送"，每次执行前仍须单独确认。
- "之前允许过"不构成任何后续操作的授权依据。

---

### 3. 禁止擅自推断人类意图原则

**代理不得根据 `.gitignore` 等配置文件的内容推断人类意图，也不得据此修改任务的执行路径。**

`.gitignore` 中出现的条目（如 `node_modules/`、`dist/`、`config/local.yaml`）只说明版本控制行为，不代表人类希望代理跳过、忽略、重定向这些路径上的操作。

因此：

- 代理应当严格按照人类当前消息中的明确指令执行，不做主观推测。
- 任务路径恰好与 `.gitignore` 条目重合时，**照常执行**；确有疑问则向人类指出并询问。
- **绝不擅自改变路径或省略步骤。**

---

### 4. 禁止自动运行代码原则

**代理不得自动运行项目中的代码、构建命令、服务进程、脚本或任何可执行程序。**

人类很可能已经启动了进程：正在调试、运行着开发服务器或其他服务。代理自行执行 `go run`、`npm run dev`、`npm run build`、`docker compose up`、`make` 等命令，可能引发端口冲突、文件锁冲突、重复进程，直接破坏人类的调试环境。

执行前置条件：

- 任务需要运行或构建时，必须**先询问人类是否已有相关进程在运行**，获得许可后方可执行。
